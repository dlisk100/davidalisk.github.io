---
const { href, title, summary } = Astro.props;
---
<a href={href} class="card p-5 hover:border-accent no-underline block text-fg">
  <h3 class="text-xl font-serif m-0">{title}</h3>
  {summary && <p class="text-sm text-muted m-0 mt-1">{summary}</p>}
</a>
